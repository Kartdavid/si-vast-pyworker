# Vast serverless PyWorker + model server deps (installed by the template start script)
vastai
torch
torchvision
# Pinned <5: BiRefNet's remote code + our Sam2 usage were written against the 4.x series;
# transformers 5.x (released mid-2026) broke model loading on the workers.
transformers>=4.56,<5
timm
einops
kornia
pillow
pillow-heif
numpy
scipy
opencv-python-headless
fastapi
uvicorn
requests
